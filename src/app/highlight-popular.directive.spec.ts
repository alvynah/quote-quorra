import { HighlightPopularDirective } from './highlight-popular.directive';

describe('HighlightPopularDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightPopularDirective();
    expect(directive).toBeTruthy();
  });
});
